import styled from 'styled-components';

const DropdownMenu = styled.div`
  position: absolute;
  right: 0;
  background-color: white;
  z-index: 100;
  box-shadow: var(--color-gray-200) 0px 8px 24px;
  color: var(--color-gray-700);
  border-radius: 6px;
  height: auto;
`;

export default DropdownMenu;
